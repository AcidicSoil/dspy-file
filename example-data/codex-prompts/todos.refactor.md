```
<!-- Placeholders:
$1 = Goal statement (e.g., "Find and group TODO/FIXME annotations")
$2 = Evidence collection steps (e.g., "Gather context by running `rg -n ...`")
$3 = Output format requirements (e.g., "- Begin with a concise summary...")
$4 = Example input (e.g., "(none – command runs without arguments)")
$5 = Expected output example (e.g., "- Group: Platform backlog — 4 TODOs referencing auth migration (owner: @platform)") -->

**CLI Task Template**

1. Goal: $1
2. Evidence: $2
3. Output: $3

Example Input: $4

Expected Output: $5
```
